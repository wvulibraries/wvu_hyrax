
<div class="wvu-flex-container">
  <% collections.reverse.each do |collection| %>
    <% col_rec = Collection.find(collection.id) %>
    <div class="wvu-flex-item">
        <%= link_to [hyrax, collection],  'aria-hidden' => true do %>
          <img src="/<%= col_rec.flex_image ? col_rec.flex_image : 'default_flex_img.jpg' %>" alt=<%= collection.title_or_label %> class="wvu-image">

          <div class="wvu-overlay">
            <div class="wvu-text"><%= collection.title_or_label %></div>
          </div>
        <% end %>
    </div>
  <% end %>
</div>

<div>&nbsp;</div>
